@model About

<h1>@Model.Title</h1>
<div class="alert alert-primary" role="alert">@Model.Description</div>


<p>Tags</p>
<ul>
@foreach(var tags in Model.Tags)
{
    <li>@tags
    </li>
}
</ul>
<div class="container text-center">
    <div class="row align-items-start">
        <div class="col-6 -sm border">
            One of three columns
        </div>
        <div class="col-2 -sm border">
            One of three columns
        </div>
        <div class="col-4 -sm border">
            One of three columns
        </div>
    </div>
</div>